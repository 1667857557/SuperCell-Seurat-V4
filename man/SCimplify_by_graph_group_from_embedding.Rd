% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SCimplify_by_graph_group.R
\name{SCimplify_by_graph_group_from_embedding}
\alias{SCimplify_by_graph_group_from_embedding}
\title{Build independent cell graphs by annotation while pooling conditions}
\usage{
SCimplify_by_graph_group_from_embedding(
  X,
  cell.graph.group,
  cell.split.condition = NULL,
  gamma = 10,
  k.knn = 5,
  n.pc = 10,
  do.approx = FALSE,
  approx.N = 20000,
  block.size = 10000,
  seed = 12345,
  igraph.clustering = c("walktrap", "louvain"),
  return.singlecell.NW = TRUE,
  return.hierarchical.structure = TRUE,
  return.group.results = FALSE,
  ...
)
}
\arguments{
\item{X}{Low-dimensional embedding with cells in rows and components in columns. Row names must be unique cell IDs.}

\item{cell.graph.group}{Cell-level graph grouping vector, typically a broad cell-type annotation. A separate graph is built for each value.}

\item{cell.split.condition}{Optional condition vector. Conditions are pooled during graph construction and used only to split mixed memberships after clustering.}

\item{gamma}{Graining level passed to \code{SCimplify_from_embedding()} within each graph group.}

\item{k.knn}{Number of neighbours used within each graph group.}

\item{n.pc}{Components of \code{X} used for graph construction.}

\item{do.approx}{Whether to use approximate coarse-graining within each graph group.}

\item{approx.N}{Approximate presample size within each graph group.}

\item{block.size}{Mapping block size for approximate coarse-graining.}

\item{seed}{Base random seed. Group \code{i} uses \code{seed + i - 1}.}

\item{igraph.clustering}{Clustering method passed to \code{SCimplify_from_embedding()}.}

\item{return.singlecell.NW}{Return the disjoint union of the independently built single-cell graphs.}

\item{return.hierarchical.structure}{Return a named list of group-specific hierarchical structures.}

\item{return.group.results}{Retain complete group-specific SuperCell results.}

\item{...}{Additional arguments passed to \code{SCimplify_from_embedding()}.}
}
\value{
A SuperCell-style list with globally unique membership IDs, graph-group provenance, and combined graph objects when their vertices can be aligned exactly to final membership IDs. Approximate construction may return \code{graph.supercells = NULL} when post-hoc condition splitting creates final memberships that are absent from the contracted presample graph. In that case \code{graph_supercells_available} is \code{FALSE} and \code{graph_supercells_unavailable_groups} identifies affected graph groups.
}
\description{
Builds one k-nearest-neighbour graph for each value of \code{cell.graph.group}. All conditions within a graph group are included in the same graph. The \code{cell.split.condition} vector is applied only after graph construction and graph clustering, so returned metacells remain condition-pure without fitting separate condition-specific graphs.
}
